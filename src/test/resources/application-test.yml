spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
#    url: jdbc:mysql://localhost:3306/sungan_test
#    password: rkdudmysql4_
#    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root

  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    show-sql: true
    database: mysql
    properties:
      hibernate:
        format_sql: true